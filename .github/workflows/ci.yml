name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  GO_VERSION: "1.25.1"

permissions:
  actions: read
  security-events: write

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v8
        with:
          version: v2.4.0
          skip-cache: false
          skip-save-cache: false

  test:
    name: Test & Quality
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        go-version: ["1.24", "1.25.1"]
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          # Fetch full history for SonarCloud
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}
          cache: true

      - name: Run tests with coverage
        run: go test -race -coverprofile=coverage.out -covermode=atomic ./...

      - name: SonarCloud Scan
        if: matrix.go-version == '1.25.1'
        uses: SonarSource/sonarqube-scan-action@v5.3.1
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}


  # integration:
  #   name: Integration Tests
  #   runs-on: ubuntu-latest
  #   # Only run integration tests on main branch or when specifically requested
  #   if: github.ref == 'refs/heads/main' || contains(github.event.head_commit.message, '[integration]')
  #   services:
  #     # If you need to start the OCR API service for integration tests
  #     # ocr-api:
  #     #   image: your-org/ocr-api:latest
  #     #   ports:
  #     #     - 8080:8080
  #     #   env:
  #     #     DATABASE_URL: postgres://test:test@postgres:5432/ocr_test
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v5

  #     - name: Setup Go
  #       uses: actions/setup-go@v4
  #       with:
  #         go-version: ${{ env.GO_VERSION }}

  #     - name: Run integration tests
  #       run: go test -tags=integration -v ./test/integration/...
  #       env:
  #         LEAPOCR_API_KEY: ${{ secrets.TEST_LEAPOCR_API_KEY }}
  #         OCR_BASE_URL: http://localhost:8080

  security:
    name: Security & Examples
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: 'npm'

      - name: Run Gosec Security Scanner
        uses: securego/gosec@master
        with:
          args: "-no-fail -fmt sarif -out results.sarif ./..."

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: go

      - name: Upload SARIF file
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: results.sarif

      - name: Build examples
        run: |
          cd examples/basic && go build .
          cd ../advanced && go build .

      - name: Install OpenAPI Generator CLI
        run: npm install -g @openapitools/openapi-generator-cli

      - name: Validate OpenAPI spec
        run: |
          make validate-spec || echo "API not running, skipping validation"


